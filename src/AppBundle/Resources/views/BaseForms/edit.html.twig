{% extends '::base.html.twig' %}

{% block content %}
    <h1>{{ 'BaseForms edit'|trans }}</h1>

    {{ form(edit_form) }}

        <a class="btn-primary btn m-y-1" href="{{ path('baseforms') }}">
           {{ 'Back to the list'|trans }}
        </a>

   {{ form(delete_form) }}

{% endblock %}
