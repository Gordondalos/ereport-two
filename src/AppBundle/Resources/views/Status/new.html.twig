{% extends '::base.html.twig' %}

{% block content%}
    <h1>{{ 'Status creation'|trans }}</h1>

    {{ form(form) }}


        <a href="{{ path('status') }}">
           {{ 'Back to the list'|trans }}
        </a>


{% endblock %}
