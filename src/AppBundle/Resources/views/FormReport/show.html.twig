{% extends '::base.html.twig' %}

{% block content%}
    <h1>FormReport</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Formname</th>
                <td>{{ entity.formName }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('formreport') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('formreport_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
