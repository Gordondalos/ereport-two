{% extends '::base.html.twig' %}

{% block content%}
    <h1>{{ 'TypeReport'|trans }}</h1>

    <table class="record_properties table">
        <tbody class="thead-default">
            <tr>
                <th>{{ 'Id'|trans }}</th>
                <th>{{ 'Typename'|trans }}</th>

            </tr>
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.typeName }}</td>
            </tr>
        </tbody>
    </table>



        <a class="btn btn-primary m-y-1" href="{{ path('typereport') }}">
            {{ 'Back to the list'|trans }}
        </a>


        <a class="btn btn-primary m-y-1" href="{{ path('typereport_edit', { 'id': entity.id }) }}">
            {{ 'Edit'|trans }}
        </a>

 {{ form(delete_form) }}

{% endblock %}
