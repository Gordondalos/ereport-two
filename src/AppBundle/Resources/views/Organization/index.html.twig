{% extends '::base.html.twig' %}

{% block content %}
    <h1>Organization list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Organizationname</th>
                <th>Organizationshortname</th>
                <th>Organizationphone</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('organization_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.organizationName }}</td>
                <td>{{ entity.organizationShortName }}</td>
                <td>{{ entity.organizationPhone }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('organization_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('organization_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('organization_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
